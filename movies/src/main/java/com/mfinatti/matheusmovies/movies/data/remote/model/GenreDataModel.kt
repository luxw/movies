package com.mfinatti.matheusmovies.movies.data.remote.model

/**
 * Models the data received from the network api/database for a movie genre.
 */
data class GenreDataModel(
    val id: Int,
    val name: String
)
