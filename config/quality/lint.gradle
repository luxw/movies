android {
    lintOptions {
        warningsAsErrors true
        abortOnError true
        htmlReport true

        lintConfig file("${rootDir}/config/quality/lint/lint-filter.xml")
        htmlOutput file("${buildDir}/reports/lint.html")
    }
}